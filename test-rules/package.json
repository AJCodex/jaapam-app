{
  "name": "jaapam-rules-tests",
  "private": true,
  "version": "0.0.0",
  "description": "Firestore security-rules tests for Jaapam. Run against the Firestore emulator.",
  "scripts": {
    "test": "jest --runInBand"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.0",
    "firebase": "^11.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.5.4"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": ["**/*.spec.ts"]
  }
}
